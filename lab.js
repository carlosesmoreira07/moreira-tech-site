/* =====================================================
   MOREIRA TECH — lab.js
   Terminal interativo de demonstração do Quality Gate.
   Carregado exclusivamente em lab.html.
   ===================================================== */

if (typeof initQualityGateTerminal === 'function') {
  initQualityGateTerminal();
}
